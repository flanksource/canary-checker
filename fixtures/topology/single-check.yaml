apiVersion: canaries.flanksource.com/v1
kind: SystemTemplate
metadata:
  name: single-check
spec:
  type: Website
  icon: Application
  schedule: "@every 5m"
  components:
    - name: single-check
      type: Website-2
      checks:
       - selector:
          labelSelector: "check=http-200"
      configs:
      - id: ["0182aa30-cd44-101f-21c3-3a0383a79254"]
        type: MSPlanner
        name: Try
