apiVersion: canaries.flanksource.com/v1
kind: Canary
metadata:
  name: mysql-pass
spec:
  interval: 30
  mysql:
    - connection: "Server=mysql.default.svc;Port=3306;Uid=$(username);Password=$(password);Database=mysqldb"
      auth:
        username:
          value: mysqladmin
        password:
          value: admin123
      query: "SELECT 1"
      results: 1

