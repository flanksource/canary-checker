apiVersion: canaries.flanksource.com/v1
kind: Canary
metadata:
  name: restic-pass-withoutinegrity
spec:
  interval: 30
restic:
  - repository: s3:https://minio.minio.svc/restic-canary-checker
    password:
      value: S0m3p@sswd
    maxAge: 1h
    accessKey:
      value: minio
    secretKey:
      value: minio123
    caCert: /home/runner/work/canary-checker/canary-checker/.certs/ingress-ca.crt
