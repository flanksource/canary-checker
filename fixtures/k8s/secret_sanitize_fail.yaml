apiVersion: canaries.flanksource.com/v1
kind: Canary
metadata:
  name: sanitize-fail
spec:
  interval: 30
  http:
    - name: Login Check
      url: https://httpbin.demo.aws.flanksource.com/
      env:
        - name: password
          value: tester123
      responseCodes: [200]
      templateBody: true
      body: |
        {
          "user": "admin{{test | ",
          "password": "{{.password}}"
        }
