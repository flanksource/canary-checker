s3:
  - buckets:
      - name: "test-bucket"
        region: "us-east-1"
        endpoint: "https://test-bucket.s3.us-east-1.amazonaws.com"
    secretKey: "****************"
    accessKey: "~~~~~~~~~~~~~~~~"
    objectPath: "path/to/object"
    skipTLSVerify: true
