apiVersion: canaries.flanksource.com/v1
kind: Canary
metadata:
  name: exec-pass
spec:
  interval: 30
  exec:
   - description: "exec dummy check"
     script: |
       echo "hello"
     test:
       expr: 'results == "hello"'